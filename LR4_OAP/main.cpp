#include "utils/functions.hpp"

int main() {
    // функция для загрузки данных в json
    jsonLoaderFunc();
    
    // функция хода игрока
    placementFunc();
    
    // функция ходов
    matchFunc();
    
    return 0;
}
